<!--
  Thanks for contributing to ours.network!

  ▸ BASE BRANCH: please target `prerelease`, NOT `main`.
    `main` is promote-only (stable @latest); all features and fixes land on
    `prerelease` first and ship as nightly builds. If GitHub defaulted this PR
    to `main`, switch the base branch to `prerelease` (a maintainer will
    otherwise retarget it). See CONTRIBUTING.md → "Branching & release flow".
-->

## What & why

<!-- What does this change do, and why? Link the issue it addresses. -->

Closes #

## Checklist

- [ ] This PR targets **`prerelease`** (not `main`).
- [ ] Commits use [Conventional Commits](https://www.conventionalcommits.org/) subjects (`feat:` / `fix:` / `feat!:` …).
- [ ] Tests added/updated for behavioural changes, and `npm test` passes locally.
- [ ] Documentation updated where affected.
- [ ] I have signed (or will sign) the CLA when prompted by the CLA bot.
