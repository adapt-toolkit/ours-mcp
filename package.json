{
  "name": "ours-mcp-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "FSL-1.1-Apache-2.0",
  "description": "ours — secure agent-to-agent communication channel over ADAPT. Agent-agnostic MCP server (packages/core) plus per-platform plugins (packages/*).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm --workspace @ours.network/mcp run dev"
  },
  "_pendingToolkitBump": "HOLD (MigrationConverge, 2026-07-16): @adapt-toolkit/mufl below is a PLACEHOLDER, not a real npm version. The migration core (submodule pins 9bb4ad1) requires a compiler that (1) ships mufl_stdlib/cryptography/e2e.mm AND (2) has META_REDUCTION_MAX_STEPS >= 1<<22 (4194304). Published 0.9.1 has NEITHER (compile fails: 'e2e.mm not found'). When the e2e-enabled @adapt-toolkit/mufl is PUBLISHED, replace the placeholder below with that exact version, regenerate package-lock.json (npm install), then this branch lands atomically with the submodule bump + actor.mu both-caps change. Do NOT push/CI until then. See PENDING-TOOLKIT-BUMP.md.",
  "devDependencies": {
    "@adapt-toolkit/mufl": "PENDING-e2e-stdlib-and-raised-ceiling"
  }
}
